<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta http-equiv="X-UA-Compatible" content="IE=edge">
		<meta name="viewport" content="width=device-width,initial-scale=1">
		<title>Raffle24.pl</title>
		<link rel="stylesheet" href="css/bootstrap.min.css" />
		<style>
			body { background: url('images/background.jpg') fixed center center; color: #eaeaea; }
			a { color: #5978a3; }
			a:hover, a:active, a:focus, a:visited { color: #6386b5; text-decoration: none; }

			.tse-scrollable{position:relative;width:100%;height:100%;overflow:hidden}.tse-scrollable .tse-scroll-content{overflow:hidden;overflow-y:scroll}.tse-scrollable .tse-scroll-content::-webkit-scrollbar{width:0;height:0}.tse-scrollbar{z-index:99;position:absolute;top:0;right:0;bottom:0;width:11px}.tse-scrollbar .drag-handle{position:absolute;right:2px;-webkit-border-radius:7px;-moz-border-radius:7px;border-radius:7px;min-height:10px;width:7px;opacity:0;-webkit-transition:opacity .2s linear;-moz-transition:opacity .2s linear;-o-transition:opacity .2s linear;-ms-transition:opacity .2s linear;transition:opacity .2s linear;background:#6c6e71;-webkit-background-clip:padding-box;-moz-background-clip:padding}.tse-scrollbar:hover .drag-handle{opacity:.7;-webkit-transition:opacity 0 linear;-moz-transition:opacity 0 linear;-o-transition:opacity 0 linear;-ms-transition:opacity 0 linear;transition:opacity 0 linear}.tse-scrollbar .drag-handle.visible{opacity:.7}.scrollbar-width-tester::-webkit-scrollbar{width:0;height:0}.tse-scrollable.horizontal .tse-scroll-content{overflow-x:scroll;overflow-y:hidden}.tse-scrollable.horizontal .tse-scroll-content::-webkit-scrollbar,.tse-scrollable.horizontal .tse-scroll-content::scrollbar{width:auto;height:0}.tse-scrollable.horizontal .tse-scrollbar{top:auto;left:0;width:auto;height:11px}.tse-scrollable.horizontal .tse-scrollbar .drag-handle{right:auto;top:2px;height:7px;min-height:0;min-width:10px;width:auto}

			.container_ { width: 100%; }
			.content { margin-left: 80px; }
			.tooltip { position: fixed; }
			.menu { position: fixed; background: rgba(25, 25, 25, 0.8); height: 100%; -webkit-user-select: none; -moz-user-select: none; z-index: 1051; }
			.menu .item { display: block; padding: 25px; margin-bottom: 1px; color: #eaeaea; }
			.menu .item_ { background: rgba(255, 255, 255, 0.1); }
			.menu .item:hover { background: rgba(255, 255, 255, 0.2); cursor: pointer; transition: ease-in 0.2s; }
			.menu .item .icon { font-size: 30px; vertical-align: middle; }
			.menu .item:hover .item_text { display: block; }
			.menu .item_text { display: none; background: #1c1c1c; position: fixed; left: 100px; margin: -40px 0 0 -20px; padding: 0 10px; line-height: 45px; border-right: 4px solid #515151; border-top: 1px solid #515151; border-bottom: 1px solid #515151; font-size: 15px; opacity: 0.8; }
			.logo { background: #475E80; height: 90px; line-height: 90px; text-align: center; border-right: 1px solid #4e698e; }
			.col_ { float: left; position: relative; }
			.items { width: 16.66666667%; }
			.bets { width: 63.333333%; }
			.chat { width: 20%; }
			.items, .bets, .chat { background: rgba(38, 38, 38, 0.96); height: calc(100vh - 90px); padding: 0; overflow: hidden; }
			.items, .bets { border-right: 1px solid #474747; }
			.panel_container { position: relative; background: #475E80; z-index: 1051; -webkit-user-select: none; -moz-user-select: none; }
			.info_bar { float: left; padding: 19px 15px 18px 15px; }
			.ar_panel { height: 90px; float: right; }
			.ar_panel .button { float: left; padding: 25px 15px 25px 15px; }
			.information_box { position: absolute; background: rgba(49, 136, 128, 0.6); left: 0; right: 0; bottom: 0; padding: 6px; margin: 0 auto; text-align: center; }
			.info_panel { float: left; margin: 0 15px 0 5px; text-align: center; }
			.info_panel .icon { font-size: 25px; vertical-align: middle; }
			.info_panel .value { font-size: 25px; font-weight: bold; vertical-align: middle; }
			.info_panel .text { display: block; font-size: 12px; text-align: center; }
			.user_menu { float: left; font-weight: normal; }
			.user_menu .user { padding: 20px; border-left: 1px solid #4e698e; }
			.user_menu .user:hover { background: #58759e; text-decoration: none; cursor: pointer; }
			.user_menu .avatar { float: left; }
			.user_menu .avatar img { width: 50px; height: 50px; border-radius: 50%; }
			.user_menu .username { float: left; margin-left: 10px; }
			.user_menu .name { font-size: 18px; }
			.user_menu .icon { font-size: 14px; }
			.user_menu .glyphicon-circle-arrow-up { display: none; }
			.show_menu { display: none; }
			.show_menu:checked ~ .user_menu .menu_ { display: block; }
			.show_menu:checked ~ .user_menu .glyphicon-circle-arrow-down { display: none; }
			.show_menu:checked ~ .user_menu .glyphicon-circle-arrow-up { display: inline-block; }
			.menu_ { display: none; background: #1c1c1c; opacity: 0.9; z-index: 101; }
			.menu_ .item { display: block; color: #eaeaea; padding: 10px; border-left : 1px solid #515151; border-bottom: 1px solid #515151; border-right: 4px solid #515151; }
			.menu_ .item:hover { background: #2b2b2b; border-right: 6px solid #848484; cursor: pointer; transition: ease-in 0.1s; }
			.menu_ .item .icon { vertical-align: middle; margin-right: 3px; font-size: 16px; }
			.items_ { padding: 15px; }
			.items_ .player { padding-bottom: 15px; border-bottom: 1px dashed #515151; }
			.items_ .player:last-of-type { padding-bottom: 0; border-bottom: 0; }
			.items_ .player:first-of-type .info { padding: 0 0 5px; }
			.items_ .player .info { padding: 15px 0 5px; }
			.items_ .player .info { font-size: 16px; color: #9b9b9b; line-height: 1.2em; }
			.items_ .player .info .name { display: block; text-transform: uppercase; font-weight: bold; font-size: 18px; }
			.items_ .player .info .description { font-size: 11px; }
			.items_ .player .info .value { font-weight: bold; }
			.items_ .player .item_list { display: block; }
			.items_ .player .item_list .item { float: left; border: 1px solid #545454; margin: 1px; padding: 5px; }
			.items_ .player .item_list .item img { width: 40px; height: 40px; }
			.items_ .player .item_list .item:first-of-type { margin-left: 0; }
			.items_ .player .item_list .item:last-of-type { margin-right: 0; }
			.chat .scroll_down { display: none; position: absolute; background: #151515; color: #848484; border: 1px solid #848484; bottom: 6px; right: 15px; padding: 10px; opacity: 0.8; z-index: 101; }
			.chat .message { display: block; padding: 10px 10px 10px 20px; line-height: 18px; word-wrap: break-word; overflow: hidden; border-bottom: 1px solid #474747; }
			.chat .message.sm { padding: 10px; font-size: 12px; color: #797979; }
			.chat .message:last-child { border-bottom: 0; }
			.chat .messages { position: relative; height: calc(100% - 34px); }
			.chat .avatar { background-repeat: no-repeat; background-size: 35px 35px; background-position-x: 10px; background-position-y: 10px; }
			.chat .message span { display: block; position: relative; padding-left: 32px; font-size: 13px; }
			.chat .message .mod { display: inline-block; padding: 0 5px 3px 5px; margin: -4px -4px 0 0; color: #6b6b6b; float: right; }
			.chat .message .mod div { vertical-align: middle; }
			.chat .message .mod div:hover { cursor: pointer; }
			.chat .new_message { width: 100%; position: absolute; bottom: 0; }
			.chat .user_message { display: none; position: absolute; width: 100%; background: rgba(19, 19, 19, 0.8); padding: 5px; bottom: 34px; overflow-wrap: break-word; }
			._input_text { display: block; width: 100%; height: 34px; padding: 6px 8px; font-size: 12px; line-height: 1.42857143; background-color: transparent; background-image: none; border: 0; -webkit-box-shadow: inset 0 1px 1px rgba(0,0,0,.075); box-shadow: inset 0 1px 1px rgba(0,0,0,.075); -webkit-transition: border-color ease-in-out .15s,-webkit-box-shadow ease-in-out .15s; -o-transition: border-color ease-in-out .15s,box-shadow ease-in-out .15s; transition: border-color ease-in-out .15s,box-shadow ease-in-out .15s; }
			._input_text:focus { border-color: #475E80; outline: 0; -webkit-box-shadow: inset 0 1px 1px rgba(0,0,0,.075),0 0 8px rgba(102,175,233,.6); box-shadow: inset 0 1px 1px rgba(0,0,0,.075),0 0 8px rgba(102,175,233,.6); }
			.circle { position: absolute; width: 600px; height: 600px; margin: auto; top: 0; left: 0; bottom: 0; right: 0; border: 1px solid #3d3d3d; border-radius: 100%; box-shadow: inset 0 1px 1px rgba(0,0,0,.075),0 0 8px rgba(102,175,233,.6); }
			.circle .value { position: relative; }
			.circle.life { animation: pulse 4s cubic-bezier(0.71, 0.1, 0.36, 0.94); animation-iteration-count: infinite; }
			.modal.in .modal_dialog { -webkit-transform: translate(0,0); -ms-transform: translate(0,0); -o-transform: translate(0,0); transform: translate(0,0); }
			.modal_dialog { width: 60%; height: 100%; margin: 0 auto; position: relative; padding: 90px 0 0 80px; transition: transform .4s ease-out; transform: translate(0,-200%); }
			.mod_dialog { position: absolute; width: 30%; height: 400px; margin: auto; padding: 10px; top: 0; right: 0; bottom: 0; left: 0; }
			.modal_content { height: 100%; position: relative; background-color: #1c1c1c; background-clip: padding-box; border-left: 1px solid #4e698e; border-right: 1px solid #4e698e; outline: 0; opacity: 0.8; }
			.mod_content { position: relative; background-color: #1c1c1c; background-clip: padding-box; border: 1px solid #4e698e; outline: 0; opacity: 0.8; }
			.modal_header { padding: 15px; text-transform: uppercase; }
			.modal_body { position: relative; padding: 0 15px 15px 15px; }
			.modal_close { float: right; font-size: 24px; font-weight: 700; line-height: 1; opacity: 0.8 !important; color: #eaeaea; -webkit-appearance: none; padding: 0; cursor: pointer; background: 0 0; border: 0; }

			@-webkit-keyframes pulse {
				50% { box-shadow: inset 0 1px 1px rgba(0,0,0,.075), 0 0 60px rgba(102,175,233,.6); }
			}

			@media (max-width: 900px) {
				.menu .item { padding: 10px 28px; }
				.menu .item .icon { font-size: 24px; }
				.user_menu { float: left; font-weight: normal; }
				.user_menu .username { display: none; }
				.menu_ { width: 90px; }
				.panel_container { height: 188px;  background: rgba(38, 38, 38, 0.96); }
			}
			@media (max-width: 1279px) {
				.modal_dialog { width: 100%; margin: 0; padding: 0 0 0 80px; }
				.items, .bets, .chat { width: 100%; height: 500px; border-right: 0; border-bottom: 1px solid #474747; }
			}
		</style>
	</head>
	<body>